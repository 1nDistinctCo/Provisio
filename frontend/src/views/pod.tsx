import React from 'react'
import { PictureList } from '../components/PictureList';
interface Props{}
const Pod:React.FC<Props> = () => {
    return (
      <div>
        <PictureList />
      </div>
    );
  };
  
  export default Pod;